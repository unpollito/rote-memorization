{
  "name": "@rote-memorization/db-client",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "i": "npx lerna bootstrap --scope @rote-memorization/db-client",
    "a": "npx lerna add --scope @rote-memorization/db-client",
    "r": "yarn remove $DEP 1>/dev/null 2>&1 || yarn clean:dependencies && yarn i",
    "build": "tsc"
  },
  "dependencies": {
    "@rote-memorization/types": "1.0.0",
    "pg": "8.5.1",
    "zapatos": "5.0.0"
  },
  "devDependencies": {
    "@types/pg": "8.6.0",
    "typescript": "4.6.3"
  }
}
